services:
  db:
    build: ./db
    image: admin_sa_db
    container_name: admin_sa_db
    ports:
      - 3306:3306
    networks:
      - sa_network
    healthcheck:
      test: ['CMD', 'mysqladmin', 'ping', '-h', 'localhost']
      interval: 10s
      timeout: 5s
      retries: 5
  server:
    build: ./server
    image: admin_sa_server
    container_name: admin_sa_server
    ports:
      - 8080:8080
    volumes:
      - ./server:/app
      - /app/node_modules
    depends_on:
      db:
        condition: service_healthy
    networks:
      - sa_network
  client:
    build: ./client
    image: admin_sa_client
    container_name: admin_sa_client
    ports:
      - 5173:5173
    volumes:
      - ./client:/app
      - /app/node_modules
    depends_on:
      - server
    networks:
      - sa_network

networks:
  sa_network:
    external: true
